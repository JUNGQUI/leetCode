package com.jk.leetcode.subrectangle;

/*
각 함수를 완성하라.

SubrectangleQueries(int[][] rectangle)
- rectangle 을 내부에서 접근 가능하게 하는 생성자
- 받아 들인 값들은 뭐가 되었든 사각형이 되었다고 생각하라

updateSubrectangle(int row1, int col1, int row2, int col2, int newValue)
- row1, col1 은 좌상단 지점, row2, col2 는 우하단 지점, 두 지점을 마우스로 드래그해서 그 안에 들어가는 값들이 범위가 된다.
- 범위 안의 값들을 모두 newValue 로 setting

int getValue(int row, int col)
- 해당 지점의 값을 가져온다.

 */